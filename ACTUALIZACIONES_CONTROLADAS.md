# Actualizaciones controladas por rol

- Administrador: canal beta automático (`/beta/`).
- Repositores: canal estable automático (raíz).
- No existe selección manual de canal.

Para probar una versión nueva, reemplazá únicamente el contenido de `beta/`. Para publicarla para todos, copiá esa versión a la raíz del repositorio.
